


let initialState = {
    plText: "Dzień dobry",
    ruText: "Добрый день",
    enText: "Good afternoon",
}

let aboutUsReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default aboutUsReducer;